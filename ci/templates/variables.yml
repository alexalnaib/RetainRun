variables:
  taskPath: task
  artifactsPath: $(Build.ArtifactStagingDirectory)
  extensionArtifactPath: $(Pipeline.Workspace)/Extension/*.vsix
  extensionPreviewVisibility: privatepreview
  extensionPreviewShareWith: alexalnaib,iag
  extensionVisibility: public
  extensionVersion: $(Build.BuildId)
  marketplaceEndpoint: vs-marketplace-con
  publisherId: alexalnaib
  extensionId: retain-run
  extensionName: 'Retain Run'
  daysToRetain: 1
