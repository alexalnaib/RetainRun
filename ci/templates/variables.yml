variables:
  taskName: RetainRun
  taskPath: Task
  sourcePath: $(Build.SourcesDirectory)
  artifactsPath: $(Build.ArtifactStagingDirectory)
  taskArtifactPath: $(Pipeline.Workspace)/RetainRun
  extensionArtifactPath: $(Pipeline.Workspace)/Extension/*.vsix
  extensionPreviewVisibility: privatepreview
  extensionPreviewShareWith: alexalnaib
  extensionVisibility: publicpreview
  extensionShareWith: ''
  tasksEndpoint: alexalnaib.tasks
  marketplaceEndpoint: vs-marketplace-con
  version: $(Build.BuildId)
  daysToRetain: 1
